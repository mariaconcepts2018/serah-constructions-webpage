/** @type {import('next').NextConfig} */
const nextConfig = {
       images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com', 'media.istockphoto.com', 'images.pexels.com'],
    },
};

export default nextConfig;

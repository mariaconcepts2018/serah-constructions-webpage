"use client";

export default function () {
  return (
    <button
      onClick={() => console.log("Clicked")}
      className="opacity-0 absolute w-full h-full bg-white curor-pointer"
    ></button>
  );
}
